# kind-config.yaml
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    extraMounts:
      - hostPath: /etc/passwd
        containerPath: /etc/passwd
      - hostPath: /etc/group
        containerPath: /etc/group
